import "package:flutter/material.dart";

class Train_Schedules extends StatelessWidget {
  const Train_Schedules({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    
    );
  }
}